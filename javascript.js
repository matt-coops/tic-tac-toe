"use strict";

const gameboard = (() => {
    const top-left = "";
    const top-right = "";
  return {};
})();

function createPlayer(name, marker) {
  return { name, marker };
}

const player1 = createPlayer("james", "X");

console.log(player1);
